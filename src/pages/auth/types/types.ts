export type DataUser = {
    status: boolean
}

export type User = {
    id: number
    email: String
    name: String
    googleId: String
    bio: String
    status: number
    createdAt: String
    updatedAt: String
    deletedAt: String
    token: String
}